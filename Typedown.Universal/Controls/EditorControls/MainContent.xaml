<UserControl
    x:Class="Typedown.Universal.Controls.MainContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.Universal.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="OnLoaded"
    SizeChanged="OnSizeChanged"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid Background="{ThemeResource SolidBackgroundFillColorTertiary}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftPaneColumn"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <local:LeftPane/>
        <ContentPresenter Grid.Column="2" x:Name="MarkdownEditorPresenter" ContextFlyout="{StaticResource EditorContextMenu}"/>
        <Border Grid.Column="1" Width="1" Background="{ThemeResource CardStrokeColorDefault}"/>
        <local:GridSplitter x:Name="Splitter" Grid.Column="1" MinWidth="40" ColumnExpectWidth="300" ColumnWidth="{x:Bind LeftPaneColumn.Width, Mode=TwoWay}"/>
    </Grid>
</UserControl>
